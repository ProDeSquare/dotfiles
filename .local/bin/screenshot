#!/bin/bash
#
# Script to capture screenshots

SCRDIR="$HOME/docx/pictures/screenshots/"

[ -d "$SCRDIR" ] || mkdir -p $SCRDIR

SCR="$SCRDIR/screenshot_full-$(date '+%y%m%d_%H%M%S').jpg"
maim -f jpg $SCR; notify-send "Maim" "Screenshot saved" --icon=$SCR
