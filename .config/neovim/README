Neovim Configs
==============

A symbolic link is already created for ``classic`` with ``~/.config/nvim``
You can manually create a symbolic link with nvim config for ``root`` user
I personally use ``classic`` variant as default nvim config, and ``root`` as
superuser's nvim config
