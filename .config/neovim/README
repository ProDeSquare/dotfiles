Neovim Configs
==============

Create a symbolic link to ``.config/nvim`` directory with any variant
I'm currently using ``root`` as the config for root user
``Classic`` mostly, and ``IDE`` for big projects
